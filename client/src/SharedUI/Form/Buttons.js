import React from 'react';

const Buttons = ({children}) => {
    return (
        <div className="sings-buttons">
            {
                children
            }
        </div>
    );
};

export default Buttons;